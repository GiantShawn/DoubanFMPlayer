{
  "name": "doubanredheart",
  "version": "1.0.0",
  "description": "",
  "main": "dist/srv/srv.js",
  "scripts": {
    "start-srv": "node srv.js",
    "start-cli": "node cli.js",
    "dev": "bash -c 'for s in srv cli lib; do babel $s --out-dir dist/$s --copy-files --source-maps inline --watch & done; wait;'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "blessed": "^0.1.81",
    "colors": "^1.1.2",
    "lodash": "^4.17.4",
    "minimist": "^1.2.0",
    "moment": "^2.18.1",
    "player": "^0.6.1"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-plugin-stack-trace-sourcemap": "^1.0.2",
    "babel-preset-env": "^1.6.0"
  }
}
